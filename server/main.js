import { Meteor } from "meteor/meteor";
import "../imports/api/rate-limit.js";
import "../imports/api/notificaciones.js";
import "../imports/api/publicaciones.js";
Meteor.startup(() => {
  // code to run on server at startup
});
